"""Port of ``random effect/ltm`` — general linear transformation model with
Gamma(2, 0.5) frailty.  Data paths and result filenames depend on the
``data_setting`` (1: ``cox_rec_rd``, 2: ``aft_rd``) to match MATLAB.
"""
